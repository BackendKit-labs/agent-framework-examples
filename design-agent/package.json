{
  "name": "@backendkit-labs/design-agent",
  "version": "0.1.0",
  "private": true,
  "description": "MCP plugin — spec-driven design lifecycle: roadmap, phases (spec→implement→verify), verification",
  "main": "dist/server.js",
  "bin": {
    "bk-design": "./dist/server.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/server.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@backendkit-labs/agent-core": "file:../../agent-framework/packages/core",
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
